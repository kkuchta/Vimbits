.row
  .span8
    %h1= @user.username.capitalize
.row
  .span4
    %hr

.bit-list
  - if @bits.length == 0
    .bit.row
      .well.span2
        No vimbits found
  - @bits.each do |bit|
    = render partial: 'bits/bit', locals: { bit: bit }
    %hr

= paginate @bits
  
